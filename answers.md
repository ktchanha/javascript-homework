1. `prompt()` გამოიყენება მომხმარებლისგან ინფორმაციის მისაღებად. ის მიღებულ მონაცემს აბრუნებს string-ის სახით.

2. თუ მომხმარებელი `prompt()`-ში შეიყვანს `42`-ს, შედეგი იქნება string `"42"` და არა number `42`, რადგან `prompt()` ყოველთვის string-ს აბრუნებს.

3. `Number()`, `parseInt()` და `parseFloat()` გამოიყენება `prompt()`-ით მიღებული string-ის რიცხვად გადასაყვანად.

4. `typeof` გვეუბნება მონაცემის ტიპს. ის სასარგებლოა debugging-ის დროს, როცა გვინდა შევამოწმოთ, რა ტიპის მონაცემთან გვაქვს საქმე.

5. `.toUpperCase()` ასოებს დიდ ასოებად აქცევს, `.toLowerCase()` — პატარა ასოებად, ხოლო `.trim()` string-ის დასაწყისსა და ბოლოში არსებულ ზედმეტ space-ებს შლის.

6. `\n` ნიშნავს ახალ ხაზზე გადასვლას, ხოლო `\t` ნიშნავს Tab-ს.

7. String არის ტექსტი, ხოლო Number არის რიცხვითი მნიშვნელობა. `"10" + 5` იძლევა `"105"`-ს, რადგან `"10"` არის string, ხოლო `10 + 5` იძლევა `15`-ს, რადგან ორივე number-ია.

8. `age >= 18` აბრუნებს Boolean მნიშვნელობას, რომელიც შეიძლება იყოს `true` ან `false`.

9. Boolean მონაცემის ტიპს აქვს ორი შესაძლო მნიშვნელობა: `true` და `false`.

10. `==` ადარებს მნიშვნელობებს ტიპების გარდაქმნით, ხოლო `===` ადარებს როგორც მნიშვნელობას, ასევე მონაცემის ტიპს, გარდაქმნის გარეშე.

11. `===` ზოგადად სასურველია, რადგან ის ამოწმებს როგორც მნიშვნელობას, ასევე მონაცემის ტიპს და უფრო პროგნოზირებად შედეგს იძლევა.

12. `!=` ამოწმებს, განსხვავდება თუ არა მნიშვნელობები ტიპების გარდაქმნით, ხოლო `!==` ამოწმებს, განსხვავდება თუ არა მნიშვნელობა ან მონაცემის ტიპი.

13. `true && false` იძლევა `false`-ს, ხოლო `true || false` იძლევა `true`-ს.

14. `!true` იძლევა `false`-ს, რადგან `!` Boolean მნიშვნელობას საპირისპიროდ ცვლის.

15. `0.1 + 0.2 === 0.3` შეიძლება იყოს `false`, რადგან ზოგი ათწილადი რიცხვი binary ფორმატში ზუსტად ვერ ინახება.

16. `&&` შეიძლება გამოვიყენოთ, როცა ორი პირობა ორივე უნდა შესრულდეს, მაგალითად, მომხმარებელი უნდა იყოს 18 წლის და ჰქონდეს ბილეთი. `||` შეიძლება გამოვიყენოთ, როცა ერთ-ერთი პირობის შესრულებაც საკმარისია, მაგალითად, მომხმარებელი უნდა იყოს სტუდენტი ან ჰქონდეს ფასდაკლების ბარათი.

17. `Math.round()` გამოიყენება რიცხვის უახლოეს მთელ რიცხვამდე დასამრგვალებლად.

18. `let`-ით შექმნილ ცვლადს შეიძლება ახალი მნიშვნელობა მივანიჭოთ, ხოლო `const`-ით შექმნილ ცვლადს ხელახლა მნიშვნელობის მინიჭება არ შეიძლება.

19. `MAX_SCORE`-ის მსგავსი სახელის დიდი ასოებით დაწერა ჩვეულებრივ მიუთითებს, რომ ეს არის მუდმივი მნიშვნელობა, რომელიც არ უნდა შეიცვალოს.
